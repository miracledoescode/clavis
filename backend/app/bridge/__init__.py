"""MT5 / MetaApi bridge — INTERNAL INFRASTRUCTURE ONLY.

No public ingress and no router: the bridge is never mounted on the FastAPI app.
The only path to it is Frontend -> FastAPI -> Bridge (CLAUDE.md). Scaffold stub.
"""
