"""Strategy JSON contract (Python half). Mirrors frontend/src/contract/types.ts.

These two files are ONE artifact in two languages — identical snake_case keys, so
the serialized JSON is byte-identical in either direction. Change one and change
the other in the SAME commit; do not add aliases. See CLAUDE.md.
"""
