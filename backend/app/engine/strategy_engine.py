"""Strategy engine: Strategy JSON evaluation and versioning.

Validates every inbound StrategySpec against app.contract.schemas before anything
reaches the agent loop, and manages monotonic strategy versions / snapshots.
Scaffold stub — not implemented.
"""
