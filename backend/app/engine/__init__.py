"""Engine package — the agent loop and strategy machinery.

Scaffold stubs only. No module logic in the skeleton: per CLAUDE.md the engine
shell precedes any module, and V0 ends at the agent_logs write (no training).
"""
